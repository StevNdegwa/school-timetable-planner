import {connect} from "react-redux";
import Settings from "../../components/MainApp/Settings";

function mapStateToProps(){
  return {
    
  }
}

const MainApp_SettingsView = connect(mapStateToProps)(Settings);
export default MainApp_SettingsView;